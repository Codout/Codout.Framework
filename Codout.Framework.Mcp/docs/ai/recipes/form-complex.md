# Form Complex Recipe

- Use `_EditOrCreate` como raiz
- Quebre em partials por subdomínio real
- Use tabs somente quando houver agrupamentos estáveis
- Mantenha lookups e dados auxiliares em `BindAsync`
- Evite despejar dezenas de campos em uma única superfície
